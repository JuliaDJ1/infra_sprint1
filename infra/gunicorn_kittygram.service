[Unit]
Description=Gunicorn daemon for Kittygram
After=network.target

[Service]
User=ubuntu
Group=www-data
WorkingDirectory=/home/ubuntu/kittygram/backend
ExecStart=/home/ubuntu/kittygram/backend/venv/bin/gunicorn --workers 3 --bind 0.0.0.0:8080 kittygram_backend.wsgi:application

Restart=always
